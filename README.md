# rag-demo
vscode
### RAG algorithm using  hugging face embedding
RAG is a technique that enhance the capablities of LLMs by combining the information retrivies with the text generation,
instaed of replying on pre-trained knowladge,RAg fetch the relevent data
from external source and use it to generate more accurate requries
